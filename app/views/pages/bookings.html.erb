
<div class="d-flex justify-content-center my-5">
  <div class="infos-card w-50 py-3 rounded-4">
      <h2 class="py-3 mb-5 text-center">📅 Mes Reservations</h2>
      <ul>
          <li class="list-group-item d-flex justify-content-evenly">

            <div><h5>🤩 Célébrités</h5> </div>
            <div><p>📅 Date de début</p></div>
            <div><p>📅 Date de fin</p></div>

          </li>
        <% current_user.bookings.each do |booking| %>
          <li class="list-group-item d-flex justify-content-evenly">

            <div><h5> <%= booking.celebrity.name %></h5></div>
            <div><p><%= booking.date_debut.strftime('%a %d %b %Y') %></p></div>
            <div ><p><%= booking.date_fin.strftime('%a %d %b %Y') %></p></div>

          </li>
          <% end %>

</ul>
</div>

</div>
